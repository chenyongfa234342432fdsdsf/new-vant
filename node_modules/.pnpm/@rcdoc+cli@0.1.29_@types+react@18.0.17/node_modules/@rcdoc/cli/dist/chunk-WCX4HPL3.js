import {
  CLI_DIR
} from "./chunk-D3YCLLHO.js";

// src/index.ts
import path from "path";
import fse from "fs-extra";
var packageJson = fse.readJSONSync(path.join(CLI_DIR, "package.json"), "utf-8");
var cliVersion = packageJson.version;
process.env.RCDOC_CLI_VERSION = cliVersion;

export {
  cliVersion
};
