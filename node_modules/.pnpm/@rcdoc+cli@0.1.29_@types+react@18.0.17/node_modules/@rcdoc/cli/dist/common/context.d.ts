import { ViteDevServer } from 'vite';
import { DefineConfig } from './defineConfig.js';
import { FSWatcher } from 'chokidar';

declare type ContextType = {
    opts?: DefineConfig;
    configFilePath?: string;
    server?: ViteDevServer;
    watchers: Array<FSWatcher>;
};
declare const context: ContextType;
declare function init(opts: any, configFilePath: string): void;
declare function updateServer(server: ViteDevServer): void;

export { context as default, init, updateServer };
