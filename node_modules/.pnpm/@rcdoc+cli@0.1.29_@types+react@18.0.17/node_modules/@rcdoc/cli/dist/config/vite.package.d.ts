import { LibraryFormats, InlineConfig } from 'vite';

declare function getViteConfigForPackage({ outputDir, minify, format, }: {
    outputDir: string;
    minify?: boolean;
    format: LibraryFormats;
}): InlineConfig;

export { getViteConfigForPackage };
