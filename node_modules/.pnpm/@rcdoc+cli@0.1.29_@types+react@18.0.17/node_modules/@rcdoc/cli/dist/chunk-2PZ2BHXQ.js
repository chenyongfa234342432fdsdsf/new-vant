import {
  smartOutputFile
} from "./chunk-PLIM7P7F.js";
import {
  SITE_SHARED_ROUTES_FILE
} from "./chunk-D3YCLLHO.js";

// src/compiler/gen-site-routes-shared.ts
import slash from "slash2";
function genExportRoutes(items) {
  return `import React from 'react';
export default [
  ${items.map(({ component, path, lang, title, redirect }) => slash(`{ path: '${path}', lang: '${lang}', title: '${title}', component: ${component}, redirect: ${redirect ? `'${redirect}'` : false} }`)).join(",\n  ")}
]`;
}
async function genSiteRoutesShared(routes) {
  const code = `
${genExportRoutes(routes)}
`;
  smartOutputFile(SITE_SHARED_ROUTES_FILE, code);
}

export {
  genSiteRoutesShared
};
