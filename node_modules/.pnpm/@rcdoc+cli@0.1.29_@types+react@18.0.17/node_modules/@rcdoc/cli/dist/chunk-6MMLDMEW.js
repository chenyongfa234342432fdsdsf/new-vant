import {
  context_default
} from "./chunk-WOQRSCMF.js";

// src/common/sigint.ts
async function killWatchers() {
  var _a;
  await Promise.all((_a = context_default) == null ? void 0 : _a.watchers.map(async (watcher) => await watcher.close()));
}
function signit() {
  process.once("SIGINT", async () => {
    var _a, _b;
    await killWatchers();
    await ((_b = (_a = context_default) == null ? void 0 : _a.server) == null ? void 0 : _b.close());
  });
}

export {
  killWatchers,
  signit
};
